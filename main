def fib(n):

        if (n<=1):
            return n
        else:
            return fib(n-2)+fib(n-1)
n=int(input("Enter the number of terms you want to print in fibonacci series: "))
for i in range(n):
    print(fib(i))
